import React from "react";
import classes from './Settings.module.css'

const Settings = () => {
    return (
        <div className={classes.settings}>
            <h2 className={classes.maintitle}>Настройки</h2>
        </div>
    )

}

export default Settings;